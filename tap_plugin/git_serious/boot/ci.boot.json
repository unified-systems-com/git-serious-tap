{
  "version": 1,
  "description": "git_serious CI record (req-boot-bootstrap-ci-record, tap#365) — the stack this plugin's own tests run in, and the one claim per-plugin CI is allowed to make: that git_serious is correct against the dependencies it DECLARES and nothing else. Install set is exactly the depends_on closure (identity_core, git_core, compliance_core, administrivia, github_core) plus git_serious itself; the consumer flips the self entry to editable so the CHECKOUT under test runs, not the pinned revision. Population seeds administrivia, git_serious (seed-only, offline). DELIBERATELY EXCLUDED: every other plugin, all credentials, all required_secrets, and any population step that reaches the network — so a fork can run this lane and a nightly cannot flake on a third party (req-boot-bootstrap-ci-record-5). Reproduce CI exactly: spawn --from 'git+https://github.com/unified-systems-com/git-serious-tap@8e390633f5db3ccd2f17a646813a3285cc30cf5a#ci' --dev-plugins git_serious",
  "install": {
    "plugins": [
      {
        "slug": "identity_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-identity-core",
          "rev": "v0.1.2"
        },
        "note": "Transitive: a dependency of github_core. Pinned at v0.1.2, the release current on 2026-09-09. Public repo, no credential (req-tap-plugin-arch-source-secret-5)."
      },
      {
        "slug": "git_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/git-core-tap",
          "rev": "v0.1.0"
        },
        "note": "Transitive: a dependency of github_core. Pinned at v0.1.0, the release current on 2026-09-09. Public repo, no credential (req-tap-plugin-arch-source-secret-5)."
      },
      {
        "slug": "compliance_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-compliance-core",
          "rev": "v0.3.0"
        },
        "note": "Transitive: a dependency of github_core. Pinned at v0.2.2, the release current on 2026-09-09. Public repo, no credential (req-tap-plugin-arch-source-secret-5)."
      },
      {
        "slug": "administrivia",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-administrivia",
          "rev": "v0.1.1"
        },
        "note": "Declared in depends_on. Pinned at v0.1.1, the release current on 2026-09-09. Public repo, no credential (req-tap-plugin-arch-source-secret-5)."
      },
      {
        "slug": "github_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-github-core",
          "rev": "v0.8.0"
        },
        "note": "Declared in depends_on. Pinned at v0.7.0 — the FLOOR this plugin declares, not merely the latest: the repository page matches github_core__pull_request and reads four github_repository fields that do not exist before v0.7.0. Public repo, no credential (req-tap-plugin-arch-source-secret-5)."
      },
      {
        "slug": "zizmor",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/zizmor-tap",
          "rev": "6f6e1406a1c2965c56fe075e761da6bc8939e65c"
        },
        "note": "Optional dependency of git_serious (the secrets panels read FLAGS_WORKFLOW findings when the scanner is installed); the CI stack installs it so that code path runs under test (req-boot-bootstrap-ci-record-3). Pinned to zizmor-tap main @ 6f6e140 (2026-09-11) — the plugin has no release tag yet."
      },
      {
        "slug": "git_serious",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/git-serious-tap",
          "rev": "8e390633f5db3ccd2f17a646813a3285cc30cf5a"
        },
        "note": "Self, pinned like any app-of-apps record — the CONSUMER (plugin-ci boot-and-test, or `spawn --dev-plugins git_serious`) substitutes the checkout under test via tap.dev_workspace (req-boot-bootstrap-ci-record-4). Booting this record unflipped runs the pinned revision."
      }
    ]
  },
  "population": {
    "on_failure": "abort",
    "steps": [
      {
        "type": "seed-plugin",
        "plugin": "administrivia",
        "enabled": true,
        "note": "administrivia's GRIFT, which this plugin's pages reference or link to."
      },
      {
        "type": "seed-plugin",
        "plugin": "git_serious",
        "enabled": true,
        "note": "Own GRIFT: seeding it proves the bundle parses and its panel types resolve against a real registry."
      }
    ]
  },
  "fips_waivers": [
    {
      "artifact": "*/bin/zizmor",
      "provider": "boringssl",
      "reason": "Carried from zizmor-tap's own ci record: zizmor is a static YAML linter for GitHub Actions workflows; the shipped Rust binary links boringssl and aws-lc-rs for its HTTPS client (fetching remote workflow references), which TAP never invokes in the operational request path — the plugin runs zizmor on local files only. No TAP data is encrypted, signed or hashed by these providers. Waived for the plugin's own test stack (zizmor-tap#10 owns the product decision: declare in [fips] or build against system OpenSSL). git_serious installs zizmor in its CI stack only because the strict validator requires the depends_on closure to be installed (optional dependency, guarded import); the product record does not install it."
    },
    {
      "artifact": "*/bin/zizmor",
      "provider": "rust-aws-lc-rs",
      "reason": "Carried from zizmor-tap's own ci record: zizmor is a static YAML linter for GitHub Actions workflows; the shipped Rust binary links boringssl and aws-lc-rs for its HTTPS client (fetching remote workflow references), which TAP never invokes in the operational request path — the plugin runs zizmor on local files only. No TAP data is encrypted, signed or hashed by these providers. Waived for the plugin's own test stack (zizmor-tap#10 owns the product decision: declare in [fips] or build against system OpenSSL). git_serious installs zizmor in its CI stack only because the strict validator requires the depends_on closure to be installed (optional dependency, guarded import); the product record does not install it."
    }
  ]
}
